"""Litmuz pipeline core.

One importable core; the REST, worker, and MCP adapters all call into it so an
MCP call and a web submission produce identical verdicts (AC-MCP-3).
"""

__version__ = "0.0.0"
